apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: webapp
spec:
  to:
    kind: Service
    name: webapp
    weight: 100
  path: /WebApp    
  port:
    targetPort: "tcp-8080"
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge


  
